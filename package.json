{
  "name": "dose-log",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Non-medical Chrome extension that records a single tap timestamp for a taken dose.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "generate:icons": "node tools/generate-icons.mjs",
    "typecheck:core": "tsc --project tsconfig.core.json --noEmit",
    "typecheck:portable": "tsc --project tsconfig.portable.json --noEmit",
    "typecheck": "npm run typecheck:core && npm run typecheck:portable && tsc --noEmit",
    "build": "npm run generate:icons && npm run typecheck && vite build"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  }
}
